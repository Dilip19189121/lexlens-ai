# LexLens AI — backend dependencies
# Upper bounds keep the set installable on Python 3.9 (the local runtime);
# newer versions can be unlocked when deploying on Python 3.10+.
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.34
pydantic>=2.6,<2.10
# 0.11.7 is the newest release whose pdfminer.six pin resolves on Python 3.9.
pdfplumber==0.11.7
httpx>=0.27,<0.29
# Newer cryptography wheels fail to load their Rust DLL on Windows + Python 3.9
# ("DLL load failed ... procedure could not be found"). 41.x works and still
# satisfies pdfminer.six (needs >=36).
cryptography<42
python-multipart>=0.0.9
python-dotenv>=1.0
